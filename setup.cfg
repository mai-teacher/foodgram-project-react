[flake8]
ignore =
    W503
    F811
exclude =
    tests/,
    */migrations/,
    venv/,
    env/,
per-file-ignores =
    */settings.py:E501
max-complexity = 10

[isort]
profile = black
# multi_line_output = 3
# include_trailing_comma = true
# force_grid_wrap = 0
# use_parentheses = True
# src_paths = foodgram-project-react/
# default_section = THIRDPARTY
src_paths = backend/
# known_third_party = django,rest_framework,setuptools
# known_django = django
# # known_third_party = rest_framework
# known_first_party = api, foodgram_backend, recipes, users
# sections = FUTURE, STDLIB, DJANGO, THIRDPARTY, FIRSTPARTY, LOCALFOLDER
